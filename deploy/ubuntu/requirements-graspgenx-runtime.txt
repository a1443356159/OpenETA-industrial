# Imports reachable from the Ubuntu image's pinned GraspGenX inference entry points.
# Training, rendering and end-to-end simulation extras are intentionally not
# installed. Torch/Torchvision come from the pinned NVIDIA base image and are
# shared read-only with this service venv.
addict
diffusers==0.11.1
h5py
huggingface-hub==0.25.2
imageio
numpy==1.26.4
omegaconf==2.3.0
PyYAML
scikit-learn
scipy
timm==1.0.15
trimesh==4.5.3
tqdm
webdataset
yourdfpy==0.0.56
